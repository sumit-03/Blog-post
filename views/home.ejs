<%- include('partials/header'); -%>

<h1>Home</h1>

<p style="color:rgb(173, 230, 187); font-size: medium;"><%= startingContent %></p>

<hr>



<% posts.forEach(function(e){ %>
    <h1><%= e.postTitle %></h1>

    <!-- truncate through html -->
    <!-- <p style="white-space: nowrap; 
    width: 50%; 
    overflow: hidden;
    text-overflow: ellipsis;"><%= e.postBody %></p> -->

    <!-- truncate through JS -->
    <% let uri = "/post/" + e.postTitle; %>
    <p><%= e.postContent.substring(0,70) + " ...." %> <a href="/post/<%= e.postTitle %>" >Read More</a></p>
<% }); %>

<%- include('partials/footer'); -%>